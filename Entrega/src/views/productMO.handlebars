

                <div class="flex items-center space-x-6">
                    <a href="#" class="text-gray-700 hover:text-black">
                        <i class="fas fa-search text-lg"></i>
                    </a>
                    <a href="#" class="text-gray-700 hover:text-black">
                        <i class="fas fa-shopping-cart text-lg"></i>
                    </a>
                    <a href="#" class="text-gray-700 hover:text-black">
                        <i class="fas fa-user text-lg"></i>
                    </a>
                </div>
            </div>
        </div>
    </nav>


        <!-- Productos -->
        <div class="container mx-auto px-4 py-12">
            <h2 class="text-2xl font-bold text-black mb-2">Productos Destacados</h2>
            <p class="text-gray-600 mb-8">Los artículos más populares de nuestra colección</p>
            
            <!-- Lista de productos -->
            <div id="productsContainer">
                <ul id="productList" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                    <!-- Los productos se cargarán dinámicamente desde Socket.IO -->
                </ul>
            </div>
        </div>
    </div>

    

    <!-- Socket.IO -->
    <script src="/socket.io/socket.io.js"></script>
    <script>
        const socket = io();

        socket.emit('requestProducts');

        // Escuchar actualizaciones de productos en tiempo real
        socket.on('productsUpdated', (products) => {
            const productList = document.getElementById('productList');
            if (!productList) return;

            // Limpiar lista actual
            productList.innerHTML = '';

            // Si no hay productos
            if (products.length === 0) {
                productList.innerHTML = `
                    <p class="text-center text-gray-600 col-span-3 py-8">No hay productos disponibles</p>
                `;
                return;
            }

            // Renderizar cada producto con el estilo del primer diseño
            products.forEach(product => {
                const li = document.createElement('li');
                li.className = "product-card bg-gray-100 shadow-md rounded-lg p-6";
                li.setAttribute('data-id', product.id);
                li.innerHTML = `
                    <div class="h-40 bg-gray-200 rounded-lg mb-4 flex items-center justify-center">
                        <span class="text-gray-500">Imagen de ${product.title}</span>
                    </div>
                    <h2 class="text-xl font-semibold text-black">${product.title}</h2>
                    <p class="text-gray-600 mt-2">Precio: $${product.price}</p>
                    <p class="text-gray-700 mt-3">${product.description}</p>
                    <div class="mt-4 flex justify-between">
                        <button class="bg-transparent border border-black text-black hover:bg-black hover:text-white font-medium py-2 px-4 rounded transition-colors">
                            Añadir al carrito
                        </button>
                        <button class="text-gray-500 hover:text-black">
                            <i class="fas fa-heart text-xl"></i>
                        </button>
                    </div>
                `;
                productList.appendChild(li);
            });
        });
    </script>